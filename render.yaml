# Render Blueprint — https://dashboard.render.com
# Option A: New + → Blueprint → select Site_Biel repo
# Option B: New + → Web Service → Root Directory: backend, Runtime: Python 3
# After deploy: set FRONTEND_URL to your Netlify URL, then redeploy.

services:
  - type: web
    name: cannahub-api
    runtime: python
    rootDir: backend
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
      - key: APP_ENV
        value: production
      - key: LLM_PROVIDER
        value: placeholder
      - key: FRONTEND_URL
        value: https://medi-canopy.netlify.app
